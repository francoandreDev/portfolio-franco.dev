import {
    blueColor,
    brickColor,
    goldColor,
    greenColor,
    pinkColor
} from './colors';
import { aboutMe, contactMe, projects, technologies } from './links';

const colors = [brickColor, goldColor, greenColor, blueColor, pinkColor];
const links = [aboutMe, projects, technologies, contactMe];

export { colors, links };
